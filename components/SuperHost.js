import React from 'react';

import styles from '../styles/SuperHost.module.css';

const SuperHost = () => {
    return (
        <div className={styles.superHostContainer}>
            SUPER HOST
        </div>
    );
};

export default SuperHost;
